import { makeStyles } from '@material-ui/core/styles';

const TemplatePopUpStyles = makeStyles({
    deleteConfirmationText: {
        color: "gray",
    },
    deleteConfirmationBody: {
        padding: "24px",
    }
});

export default TemplatePopUpStyles;