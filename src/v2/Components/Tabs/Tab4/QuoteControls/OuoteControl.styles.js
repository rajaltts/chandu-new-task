import { makeStyles } from '@material-ui/core'

const quoteControlStyles = makeStyles(() => ({
    mainBox: {
        marginTop: '20px',
    },
    selectSeparationWrapper: {
        margin: '16px 0px',
    },
}))

export default quoteControlStyles
